# Phase Gate

A feature or group (Nhóm) is complete only when implementation, verification, and evidence all exist.

Within a group, V1 ships before V2/V3 (see `product.md`). Do not pull a later group or a later version into current scope without explicit scope from the user — re-read the active entry in `feature_list.json` instead of expanding on your own.
